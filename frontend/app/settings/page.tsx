"use client";
export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-2xl text-lexi-accent font-mono mb-4">SETTINGS</h1>
      <p className="text-lexi-muted">Edit <code>.env</code> on the server to change provider keys, voice and vision backends.</p>
    </div>
  );
}
