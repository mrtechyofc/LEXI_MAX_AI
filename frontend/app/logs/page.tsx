"use client";
export default function LogsPage() {
  return (
    <div>
      <h1 className="text-2xl text-lexi-accent font-mono mb-4">LOGS</h1>
      <p className="text-lexi-muted">Connect to <code>/metrics</code> via Prometheus + Grafana for dashboards.</p>
    </div>
  );
}
