apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexi-backend
  namespace: lexi
spec:
  replicas: 2
  selector: { matchLabels: { app: lexi-backend } }
  template:
    metadata: { labels: { app: lexi-backend } }
    spec:
      containers:
        - name: backend
          image: ghcr.io/yourorg/lexi-backend:latest
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: lexi-config }
            - secretRef:    { name: lexi-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "2Gi" }
---
apiVersion: v1
kind: Service
metadata: { name: lexi-backend, namespace: lexi }
spec:
  selector: { app: lexi-backend }
  ports: [{ port: 8000, targetPort: 8000 }]
