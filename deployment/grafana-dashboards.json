{
  "title": "LEXI Overview",
  "panels": [
    {"type": "stat", "title": "Request rate", "targets": [{"expr": "rate(http_requests_total[1m])"}]},
    {"type": "stat", "title": "LLM latency p95",  "targets": [{"expr": "histogram_quantile(0.95, sum(rate(lexi_llm_latency_seconds_bucket[5m])) by (le))"}]}
  ]
}
